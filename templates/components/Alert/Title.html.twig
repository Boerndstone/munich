{# @block content Short alert heading #}
<div
	data-slot="alert-title"
	class="{{ ('font-semibold leading-none tracking-tight ' ~ attributes.render('class'))|tailwind_merge }}"
	{{ attributes.without('class') }}
>
	{%- block content %}{% endblock -%}
</div>
