# Copy to .env.local and adjust if you want to override only these when using Docker.
# With docker-compose.yml we set DATABASE_URL via environment, so this is optional.

# DATABASE_URL=mysql://munich:munich_secret@mysql:3306/munich?serverVersion=8.0&charset=utf8mb4
# APP_ENV=dev
