{% extends 'base.html.twig' %}

{% block title %}Klettergebiete um München
{% endblock %}

{% block body %}
	{{ include('components/layout/navigation.html.twig') }}
	<main id="main-content">
		<div class="mx-auto my-4 w-full max-w-[1024px] px-2 sm:px-4">
			<section>
				<div class="mb-3 flex items-center justify-between py-3">
					<h1 class="mb-0 text-3xl font-medium leading-tight text-gray-900 dark:text-gray-100">Neuste Routen</h1>
				</div>
				{{ include('components/page/latest-routes-page-table.html.twig', { latestRoutes: latestRoutes }) }}
			</section>
		</div>
	</main>
{% endblock %}
